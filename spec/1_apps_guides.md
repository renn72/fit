# apps guides

1. i want to build out a  group of md files, to help guide the process of each app/package withing the monorepo
2. store each on the `../guides` dir
3. read through `../API.md` `../SESSION_LOG_fit.md` to get an understanding of the progress of the monorepo
4. i want a md document for each app and package
5. each document should be concise, prefering sub headings and lists
6. anything client facing should also include a style section, outlining goals and approaches
7. update the AGENTS_LOOP.md to include each created md, and add a 2 new step to the AGENTS_LOOP that when updating a relevant app, to review the relevant md, and after completing a task, update the md if the action has extended the scope or understanding of the relevant app.



