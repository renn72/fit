# Session Log - February 18, 2026

[[fit]]

## Tasks Completed
- Created recipe API schema at `packages/api/src/schemas/recipe.ts` with input validation for:
  - `RecipeGetInput` - Get a single recipe by ID
  - `RecipeGetAllOrgInput` - Get all recipes for an organization
  - `RecipeGetAllAdminInput` - Get all recipes (admin/dictator only)
  - `RecipeCreateInput` - Create a new recipe
  - `RecipeUpdateInput` - Update an existing recipe
  - `RecipeDeleteInput` - Delete a recipe
- Implemented recipe router at `packages/api/src/routers/recipe.ts` with 6 procedures:
  - `get` - Get a single recipe by ID with organization access control
  - `getOrg` - Get all recipes for an organization with permission checks
  - `getAllAdmin` - Get all recipes across all organizations (dictator only)
  - `create` - Create a new recipe (requires `itemUpdater` or `dictator` metatag)
  - `update` - Update an existing recipe (requires `itemUpdater` or `dictator` metatag, must belong to user's org)
  - `delete` - Delete a recipe (requires `itemUpdater` or `dictator` metatag, must belong to user's org unless dictator)
- Registered `recipeRouter` in `packages/api/src/routers/index.ts`
- Recipes include relations to ingredients via `recipeToIngredient` junction table
- All procedures follow existing patterns from ingredient/exercise routers:
  - Proper metatag authorization checks
  - Organization-based access control
  - Dictator bypass for cross-organization access
  - Relational queries with creator and ingredient data
